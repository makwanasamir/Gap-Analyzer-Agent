# Production dependencies for Azure App Service
botbuilder-core>=4.16.0
botbuilder-schema>=4.16.0
botbuilder-integration-aiohttp>=4.16.0
teams-ai>=1.0.0
aiohttp>=3.9.0
gunicorn>=21.2.0

# Azure OpenAI (Legacy client if needed, or via teams-ai)
# teams-ai usually handles this but we kept custom client for now
openai>=1.12.0

# File processing
pypdf>=6.6.2
python-docx>=1.1.0
httpx>=0.27.0

# Token counting
tiktoken>=0.7.0

# Configuration
python-dotenv>=1.0.0
